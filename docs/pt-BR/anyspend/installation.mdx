---
title: Instalação & Configuração
description: 'Comece com o AnySpend - instalação, configuração básica e verificação'
lang: pt-BR
originalPath: anyspend/installation.mdx
---
```html
<iframe class="w-full aspect-video rounded-xl mb-6" src="https://customer-gg6qs7nm5ue94t64.cloudflarestream.com/66d9ebc2e3a14ec87dfa360b83e26900/iframe?poster=https%3A%2F%2Fcustomer-gg6qs7nm5ue94t64.cloudflarestream.com%2F66d9ebc2e3a14ec87dfa360b83e26900%2Fthumbnails%2Fthumbnail.jpg%3Ftime%3D168%26height%3D600" title="Configuração do AnySpend" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen />
```

## Pré-requisitos

```jsx
<CardGroup cols={3}>
  <Card title="Node.js" icon="node-js">
    v20.15.0 ou superior
  </Card>
  <Card title="React" icon="react">
    Versão 18 ou 19
  </Card>
  <Card title="TypeScript" icon="code">
    Recomendado para uma melhor DX
  </Card>
</CardGroup>
```

## Instalação

Escolha o seu gerenciador de pacotes preferido:

```jsx
<Tabs>
  <Tab title="npm">
    ```bash
    npm install @b3dotfun/sdk
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm add @b3dotfun/sdk
    ```
  </Tab>
</Tabs>
```

## Configuração Básica

### 1. Configuração do Provider

Envolver seu aplicativo com o `B3Provider` e `AnyspendProvider` para habilitar a funcionalidade AnySpend:

```tsx App.tsx icon=react
import { AnyspendProvider } from "@b3dotfun/sdk/anyspend/react";
import { B3Provider } from "@b3dotfun/sdk/global-account/react";
import "@b3dotfun/sdk/index.css";

function App() {
  return (
    <B3Provider theme="light" environment="production">
      <AnyspendProvider>{/* Seus componentes do aplicativo */}</AnyspendProvider>
    </B3Provider>
  );
}

export default App;
```

### 2. Configuração do Ambiente

```jsx
<Tabs>
  <Tab title="Mainnet">
    **Endpoint**: `https://mainnet.anyspend.com`

    Use isso para aplicações em produção com transações reais.
  </Tab>
  <Tab title="Testnet">
    **Endpoint**: `http://testnet.anyspend.com`

    Use isso para desenvolvimento e testes com tokens de teste.

    Nota: Testnet atualmente não está disponível.
  </Tab>
</Tabs>
```

### 3. Configuração do TypeScript (Opcional mas Recomendado)

```jsx
<Note>
  Se você está usando TypeScript, garanta que seu `tsconfig.json` inclua essas configurações para compatibilidade ótima:
</Note>

```json tsconfig.json icon=settings
{
  "compilerOptions": {
    "moduleResolution": "node",
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true
  }
}
```

## Verificação

Crie um componente de teste simples para verificar se sua configuração funciona corretamente:

```tsx TestComponent.tsx icon=test-tube lines
import { AnySpend } from "@b3dotfun/sdk/anyspend/react";

function TestComponent() {
  return <AnySpend />;
}
```

## Próximos Passos

```jsx
<CardGroup cols={3}>
  <Card title="Explorar Componentes" icon="puzzle-piece" href="/anyspend/components">
    Aprenda sobre os componentes disponíveis para diferentes cenários de pagamento
  </Card>
  <Card title="Aprender sobre Hooks" icon="link" href="/anyspend/hooks">
    Descubra hooks do React para construir fluxos de pagamento personalizados
  </Card>
  <Card title="Ver Exemplos" icon="code" href="/anyspend/examples">
    Navegue por exemplos de implementação do mundo real
  </Card>
</CardGroup>
```

## Solução de Problemas

```jsx
<AccordionGroup>
  <Accordion title="Erros de Importação">
    Certifique-se de que você instalou o SDK corretamente e importou o arquivo CSS. O SDK requer React 18\+ e pode ter problemas de compatibilidade com versões anteriores.
  </Accordion>
  <Accordion title="Provider Não Encontrado">
    Garanta que você tem `B3Provider` e `AnyspendProvider` colocados no alto da sua árvore de componentes, tipicamente no seu componente principal App.
  </Accordion>
  <Accordion title="Erros de TypeScript">
    Se você está vendo erros de TypeScript, verifique se seu `tsconfig.json` inclui as configurações recomendadas acima.
  </Accordion>
</AccordionGroup>
```
