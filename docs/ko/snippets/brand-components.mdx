---
lang: ko
originalPath: snippets/brand-components.mdx
---
```jsx
export const B3ColorPalette = () => (
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))",
      gap: "1rem",
      margin: "2rem 0",
    }}
  >
    <div style={{ textAlign: "center" }}>
      <div
        style={{
          backgroundColor: "#007AFF",
          height: "80px",
          borderRadius: "8px",
          border: "1px solid #e5e7eb",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          color: "white",
          fontWeight: "bold",
        }}
      >
        Primary
      </div>
      <h4 style={{ margin: "0.5rem 0" }}>프라이머리 블루</h4>
      <code style={{ fontSize: "0.875rem" }}>{"#007AFF"}</code>
    </div>

    <div style={{ textAlign: "center" }}>
      <div
        style={{
          backgroundColor: "#6B7280",
          height: "80px",
          borderRadius: "8px",
          border: "1px solid #e5e7eb",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          color: "white",
          fontWeight: "bold",
        }}
      >
        그레이
      </div>
      <h4 style={{ margin: "0.5rem 0" }}>그레이</h4>
      <code style={{ fontSize: "0.875rem" }}>{"#6B7280"}</code>
    </div>

    <div style={{ textAlign: "center" }}>
      <div
        style={{
          backgroundColor: "#FFFFFF",
          height: "80px",
          borderRadius: "8px",
          border: "2px solid #e5e7eb",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          color: "black",
          fontWeight: "bold",
        }}
      >
        화이트
      </div>
      <h4 style={{ margin: "0.5rem 0" }}>화이트</h4>
      <code style={{ fontSize: "0.875rem" }}>{"#FFFFFF"}</code>
    </div>
  </div>
);

export const B3FontShowcase = () => (
  <div style={{ margin: "2rem 0" }}>
    <div style={{ marginBottom: "2rem" }}>
      <h3>Neue Montreal Family</h3>
      {[
        "Neue Montreal Bold",
        "Neue Montreal Regular",
        "Neue Montreal Book",
        "Neue Montreal Medium",
        "Neue Montreal Semibold"
      ].map(font => (
          <div
            key={font}
            style={{
              fontFamily: `"${font}", sans-serif`,
              fontSize: "18px",
              margin: "1rem 0",
              padding: "0.5rem",
              border: "1px solid #e5e7eb",
              borderRadius: "4px",
            }}
          >
            <strong>{font}:</strong> 빠른 갈색 여우가 게으른 개를 뛰어넘습니다
          </div>
        ))}
    </div>

    <div style={{ marginBottom: "2rem" }}>
      <h3>Calibre Family</h3>
      {[
        "Calibre Bold",
        "Calibre Heavy"
      ].map(font => (
          <div
            key={font}
            style={{
              fontFamily: `"${font}", sans-serif`,
              fontSize: "18px",
              margin: "1rem 0",
              padding: "0.5rem",
              border: "1px solid #e5e7eb",
              borderRadius: "4px",
            }}
          >
            <strong>{font}:</strong> 게이밍은 온체인 경험의 미래입니다
          </div>
        ))}
    </div>
  </div>
);

export const B3BrandValues = () => (
  <div
    style={{
      textAlign: "center",
      margin: "2rem 0",
      padding: "2rem",
      backgroundColor: "#f8fafc",
      borderRadius: "8px",
      fontWeight: "bold",
      letterSpacing: "2px",
      fontSize: "1.1rem",
    }}
  >
    ✦ 커뮤니티 주도 ✦ 재미 ✦ 생태계 조화 ✦
  </div>
);

export const LogoShowcase = ({ variant = "blue", size = 120 }) => {
  const logoSrc =
    variant === "blue"
      ? "/logo/light.svg"
      : variant === "white"
        ? "/logo/white.svg"
        : "/logo/black.svg";

  const containerStyle =
    variant === "white"
      ? { backgroundColor: "#000", padding: "2rem", borderRadius: "8px", textAlign: "center" }
      : { padding: "2rem", borderRadius: "8px", textAlign: "center", border: "1px solid #e5e7eb" };

  return (
    <div style={containerStyle}>
      <img
        src={logoSrc || "/logo/light.svg"}
        alt="B3 Logo"
        style={{ maxWidth: `${size}px`, height: "auto" }}
      />
    </div>
  );
};

export const QuickBrandGuide = () => (
  <div
    style={{
      padding: "1.5rem",
      backgroundColor: "#f0f9ff",
      borderRadius: "8px",
      border: "1px solid #0ea5e9",
      margin: "1rem 0",
    }}
  >
    <h4 style={{ margin: "0 0 1rem 0", color: "#0369a1" }}>빠른 브랜드 구현</h4>
    <ul style={{ margin: 0, paddingLeft: "1.5rem" }}>
      <li>
        <strong>색상:</strong> 주 색상으로 {"#007AFF"} 사용, 생동감 있는 악센트 적용
      </li>
      <li>
        <strong>타이포그래피:</strong> {"https://cdn.b3.fun/fonts.css"} 가져오기
      </li>
      <li>
        <strong>로고:</strong> 모든 면에서 높이의 0.5배 크기의 여백 유지
      </li>
      <li>
        <strong>톤:</strong> 재미있고, 커뮤니티 주도적이며, 게이밍에 초점을 맞추세요
      </li>
    </ul>
  </div>
);

export const BrandCodeExample = ({ language = "css" }) => {
  const examples = {
    css: `/* B3 브랜드 구현 */
@import url("${"https://cdn.b3.fun/fonts.css"}");

:root {
--b3-primary: ${"#007AFF"};
--b3-grey: ${"#6B7280"};
--b3-white: ${"#FFFFFF"};
}

body {
font-family: ${"\"Neue Montreal Medium\", sans-serif"};
}

.b3-button {
background-color: var(--b3-primary);
color: var(--b3-white);
font-family: ${"\"Calibre Bold\", sans-serif"};
border-radius: 8px;
padding: 0.75rem 1.5rem;
border: none;
cursor: pointer;
transition: transform 0.2s ease;
}

.b3-button:hover {
transform: scale(1.05);
}`,

    react: `import { branding } from '/snippets/variables';

function B3Button({ children, onClick }) {
return (
<button
onClick={onClick}
style={{
        backgroundColor: "#007AFF",
        color: "#FFFFFF",
        fontFamily: "\"Calibre Bold\", sans-serif",
        borderRadius: '8px',
        padding: '0.75rem 1.5rem',
        border: 'none',
        cursor: 'pointer',
        transition: 'transform 0.2s ease'
      }}
onMouseEnter={(e) => e.target.style.transform = 'scale(1.05)'}
onMouseLeave={(e) => e.target.style.transform = 'scale(1)'} >
{children}
</button>
);
}`,

    tailwind: `// tailwind.config.js

module.exports = {
theme: {
extend: {
colors: {
'b3-blue': '${"#007AFF"}',
        'b3-grey': '${"#6B7280"}',
},
fontFamily: {
'b3-primary': ['Neue Montreal Medium', 'sans-serif'],
'b3-secondary': ['Calibre Bold', 'sans-serif'],
}
}
}
}

<!-- 사용법 -->

<button class="bg-b3-blue font-b3-secondary rounded-lg px-6 py-3 text-white transition-transform hover:scale-105">
  게임 온!
</button>
` }; return (
<pre
  style={{
    backgroundColor: "#1e293b",
    color: "#f1f5f9",
    padding: "1rem",
    borderRadius: "8px",
    overflow: "auto",
    fontSize: "0.875rem",
    lineHeight: "1.5",
  }}
>
  <code>{examples[language] || examples.css}</code>
</pre>
); };
```
